The `yavashark_interpreter` crate is the interpreter for the YavaShark engine. It contains everything to interpret the AST generated by the swc parser.
It only is a tree walker, so it isn't really fast